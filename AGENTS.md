# AGENTS

适用范围：本公开版仓库。

## 公开边界

- 这里是公开版，不要提交私有域名、服务器 IP、密码、证书、签名文件，或任何只该出现在私有版的部署细节。
- 不要加入 `environment_report.php`、`device_report.php`、激进设备探测、已安装应用包名上传，或其他私有环境/设备上报链路。
- 可以保留本地环境风险检测，但默认不要把已安装应用包名列表上传到服务端。
- `android-client/assets/server-url.txt` 和 `android-admin-client/assets/server-url.txt` 在公开仓库中必须保持空白；本地构建可以临时填写自己的地址，但不得提交。

## 功能契约

- 新生成的家庭组号是 8 位小写英文字母或数字；迁移前已有的 32 位小写十六进制组号保存在唯一 `legacy_group_code` 中并继续可用。注册和加入家庭组必须同时接受两种格式，响应只展示当前 8 位组号。
- 后台留空创建邀请码时默认生成 8 位小写英数字；自定义邀请码允许 4 至 64 位英数字。注册兑换仍兼容已有的 1 至 255 位英数字邀请码。
- 普通明文历史记录由服务端按家庭组、成员和坐标系分区，以每次停留的首个点为锚，在 25 米以内做只读合并；不得删除或改写原始定位记录。返回值必须包含首次上报、末次上报、停留秒数和上报次数。
- P2P 历史只有在服务端返回完整 `client_merge_snapshot` 时，才允许客户端解密后重新执行 25 米合并，并在合并后重算分页和每成员地图上限；快照不完整时必须保留原始行并明确提示，不能局部合并。
- 历史地图只展示 GPS 位置，不绘制 IP 或 WebRTC 标记；地图下方的详细数据与历史位置文本继续保留 IP/WebRTC 地址、区县、街道、详情、POI、邮编和坐标系等字段。
- 主页高德地图在 App 前台切换页签时应复用同一个 `homeMapWebView`；动态行清理只能把它从旧父容器分离，只有 `onStop`、`onDestroy` 或已退后台的内存回收才释放。
- 手动位置上报使用 `ReportAttemptGate` 隔离新旧回调，并由全链路 watchdog 收口；成功、失败、异常、超时和退后台都必须幂等清理定位监听与 watchdog，并同步“上报当前位置”按钮状态。

## 构建与验证

- 发布前优先运行 `.\verify-public.ps1`，它是公开边界的首选回归检查。
- Android 相关工作默认走命令行脚本，例如 `.\android-client\build.ps1` 和 `.\android-admin-client\build.ps1`，不要假设需要 Android Studio。
- 从私有树同步改动过来时，先删掉秘密、私有 URL、受保护下载路径和只在私有版成立的行为，再提交到公开版。

## 同步约定

- 从 `v2/` 或其他私有树同步时，优先同步用户可见功能、通用 bugfix 和不含隐私风险的验证脚本。
- 任何涉及私有部署、服务器后台、环境指纹或上传端点的实现，都应先证明公开化后仍然安全，否则不要同步。
- 从旧版同步家庭组号迁移时，不得删除 32 位兼容别名或重新轮换已经生成的 8 位当前组号。
